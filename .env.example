# Frontend (Vite) build-time env
# VITE_* variables are exposed to browser bundle.
VITE_API_URL=http://localhost:8000

# Docker (PostgreSQL)
DB_PORT=5432
POSTGRES_DB=roombook
POSTGRES_USER=roombook
POSTGRES_PASSWORD=roombook
